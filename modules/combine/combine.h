// Provide prototypes for functions which will combine all the remaining modules into one

int genesis(char* name, game_character charachter);
int apocalypse(); // Apocalypse will never arrive on its own. It will be invoked by the will of the man (end user) 